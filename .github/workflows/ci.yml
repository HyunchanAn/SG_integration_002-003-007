name: Integrated Surface Analysis CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Python 3.10
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"
        cache: "pip"

    - name: Install Lint Tools
      run: |
        python -m pip install --upgrade pip
        pip install ruff pytest

    - name: Run Ruff Linter
      run: |
        ruff check . --exit-zero

    - name: Install Dependencies
      run: |
        # CI 빌드 속도 향상을 위해 PyTorch 및 대형 GPU 의존성은 CPU 버전으로 설치
        pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
        # SAM 2.1 등의 패키지 및 requirements.txt 의존성 설치
        pip install -r requirements.txt

    - name: Run Import Tests
      run: |
        pytest tests/test_imports.py

  docker-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    - name: Build Docker Image (Verify Only)
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./Dockerfile
        push: false
        tags: sg-integration-app:latest
        # 캐시 활용하여 CI 시간 단축
        cache-from: type=gha
        cache-to: type=gha,mode=max

  # ---------------------------------------------------------------------------
  # CD 파이프라인 (자동 배포 및 이미지 푸시 - 활성화 가이드는 README.md 참고)
  # ---------------------------------------------------------------------------
  # deploy-ghcr:
  #   needs: [lint-and-test, docker-build]
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #
  #   - name: Log in to GitHub Container Registry (GHCR)
  #     uses: docker/login-action@v3
  #     with:
  #       registry: ghcr.io
  #       username: ${{ github.actor }}
  #       password: ${{ secrets.GITHUB_TOKEN }}
  #
  #   - name: Build and Push to GHCR
  #     uses: docker/build-push-action@v5
  #     with:
  #       context: .
  #       file: ./Dockerfile
  #       push: true
  #       tags: |
  #         ghcr.io/${{ github.repository_owner }}/sg-integration-app:latest
  #         ghcr.io/${{ github.repository_owner }}/sg-integration-app:${{ github.sha }}
  #       cache-from: type=gha
  #       cache-to: type=gha,mode=max
